{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Create virtual environment",
            "type": "shell",
            "command": "python3 -m venv env && source env/bin/activate",
            "presentation": {
                "reveal": "always"
            },
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}